The function testForensics2D.m takes input as the string which contains the path to the image. The input image will be shown on the screen, first select the portion of the image that is tampered, and then select the portion of the non-tampered image. 

Demo code is given in runTests.m